name: pokeflutter
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: 0.12.2
  provider: 4.3.2+2
  get_it: 4.0.4
  hive: ^1.4.4
  hive_flutter: ^0.3.1

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.8.0

flutter:
  uses-material-design: true

  assets:
    - assets/pokeball.png
    - assets/pokemon_error.jpg
    - assets/intro1.jpg
